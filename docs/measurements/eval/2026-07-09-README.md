# First P19 competence measurement — 2026-07-09

Model-under-test: a Sonnet-class agent session, spec pack as sole language reference, no
toolchain self-checking (first-attempt discipline), all 12 seed tasks, scored by the harness
against the real toolchain.

**Result: 12/12 first attempt (generate 8/8, repair 4/4); round-2 rescore identical; slope
delta 0 — the seed set is saturated at ceiling for this model class.**

Findings recorded:
1. The seed task set cannot measure slope for strong models — the graduation set (full basket
   sub-programs, harder compositions) is the needed next increment before any cross-model or
   cross-release comparison means anything.
2. One concrete pack gap identified and fixed: contract-clause placement had no directly-stated
   example (the model inferred it from an unrelated alloc example); grammar.md now shows the
   canonical form.
3. Process note: the model-under-test reported injection-like reminder text "embedded in
   diagnostics.md"; inspection shows the repo files clean — the text was a harness-context
   artifact of its own session misattributed to the file. The agent's report-don't-act response
   was correct; recorded here so future eval runs expect and disregard the same artifact class.

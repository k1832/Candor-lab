# Joint adversarial review #1 of designs 0007 (generics) and 0008 (modules)

**Date:** 2026-07-08. **Verdicts: both (b).** The type-position bracket collision audit held
completely (the no-angle-bracket refusal pays off as claimed); the orphan-as-lookup, opaque-T
conservatism, and 0008's two-hash P20 split (for declared effects) all survived. Eleven findings.

Dispositions (deciding authority's session) — all accepted; designed resolutions where the
reviewer left them open:

**F1 (top, bracket seam):** mixed lists disambiguated by declaration keyword — region parameters
are declared `region r` in the bracket list (`fn choose[region r, T](a: read[r] T)`); bare
identifiers in the list are always type parameters; the compact-default trigger becomes "no
region declarations". Rare signatures carry the weight, per P12. 0006/0001 §3.3 gain the note.
**F2:** direct self-instantiation with a growing type is a def-site error where decidable; a
documented deterministic monomorphization depth limit backstops the undecidable remainder —
classified honestly as a compile resource limit, with NN#10's letter (no TYPE errors at
instantiation) preserved and the residual named.
**F3:** generic-function values spelled `name::[T]` — the `::[` digraph is unambiguous (`::` is
already reserved), rare, and NN#13-clean; covers let-bindings, fn-pointer slots, and vtables.
**F4:** the coherence key is the INSTANTIATED interface: uniqueness over (I[args...], T); the
seam predicate refers to the interface DECLARATION's module. From[E1]/From[E3] coexist.
**F5:** resolved in the check-once direction: generic drop-glue alloc-ness is fixed at the def
site conservatively (T-dropping ⇒ alloc for all instances, ground-floor tax bites at Wrap[u8]);
0008 §5.2's resolved-at-codegen claim corrected — effects are never re-derived at codegen.
**F6:** manifest honesty corrected (identity + optional freestanding claim; lockfile named as
deferred package-manager scope). **F7:** cross-type ?'s effect inheritance disclosed. **F8:**
conv-with-type-param stated unwritable by design, consistent with the no-Num-bound refusal.
**F9:** self counts as the sole borrow-in for the interface compact default; impl region
signatures must match the interface exactly. **F10:** discharged by scoping — the prototype does
not implement generics; sizeof(T) is legal only post-monomorphization. **F11:** the
declaration-site bracket rule stated explicitly.

# Adversarial review #1 of designs 0004 (field_ptr) and 0005 (implicit reborrow)

**Date:** 2026-07-08
**Reviewer:** independent LLM session (heavy tier); verified both docs' evidence against the port
code (0004's scheduler census accurate; 0005's 296-site count reproduced exactly), attacked the
audit-line and no-semantics-change claims (both held), and the field_ptr × implicit-reborrow
interaction (no composite hazard). **Verdicts: both (b) — sound after named amendments.** No
memory-safety hole in either; the defects are process integrity and cross-document consistency.

Dispositions (deciding authority's session): **all nine findings accepted.**

1. **PROCESS (0004): table_version bump is frozen-criterion-prohibited and unnecessary.**
[Disposition: accepted exactly as proposed. The bump framing is deleted. The metric is
region-based (§4.1 valve statements = statements intersecting unsafe regions), so a safe op
auto-excludes with nothing to classify. The pre-record becomes a **§9 philosophy-tier ruling**
(above the criterion, not a modification of it) that field_ptr is safe-hence-uncounted, recorded
in ADJUDICATIONS.md under an explicit philosophy-tier heading BEFORE any re-port code exists;
plus the named requirement that the counter be updated to parse the new grammar with a test
demonstrating the frozen region rule is preserved (cross-counter fixture extended).]

2. **MAJOR (0005): the reclassification is a demotion and the mutation-audit loss is a real P2 cost.**
[Disposition: accepted. 0005 names the mutation-point-audit locality loss as an explicit P2
concession in the documented-limitation style, flags that it argues the less-guarded
reclassification direction, and weighs option (d) against 0004's greppability rationale on the
record. The decision stands (the signature carries the bit; the basket's 296 sites are the
measured cost of the alternative), but as an argued trade, not a settled matter.]

3. **MAJOR (0005): contradicts hash-frozen 0003 conservatism #4; tripwire fires.**
[Disposition: accepted. 0003 is updated in the same change series: conservatism #4 rewritten,
the desugaring rule documented in §2.2, and 0003 re-reviewed by a fresh session as part of the
implementation verification pass before the re-port.]

4. **MAJOR (0005): type-aware formatter requirement and must-not-rewrite cases unstated.**
[Disposition: accepted. Requirement stated with the enumerated non-rewrite cases (take-mode
borrow-typed params — where reborrow vs move genuinely differ — and non-place arguments).
Prototype note added: the prototype has no formatter; both forms parse there, the re-port is
written in the canonical bare form, and the formatter obligation binds the real toolchain (P16).]

5. **MINOR (0004): E0510 is a new checker rule; 0003 §2.5 enumeration edit named.** [Accepted.]

6. **MINOR (0004): impact restated in the frozen metric — ≈1 valve statement (t_link); the two
"shrinks" are metric-invisible.** [Accepted; honesty improved by the correction.]

7. **MINOR (0005): rule 1's `p.f` example violates §3.4.** [Accepted; example replaced.]

8. **MINOR (0005): temporary-reborrow residual means "exactly one form" over-claims.**
[Accepted; residual stated.]

9. **MINOR (0005): "use-after-move" characterization loosened to "ill-formed (type-mismatch or
use-after-move by shareability)"; conclusion (pure legalization) verified and unchanged.** [Accepted.]

**What held under attack, for the record:** 0004's audit-line safety argument (typed rawptr is
trusted only at the gated deref; no safe consumer newly misled); 0004's census; 0004's
reclassification (legitimate direction); 0005's no-silent-behavior-change claim (verified across
shared/exclusive); 0005's Stage-3 composition incl. the S1 shape; the cross-feature interaction.

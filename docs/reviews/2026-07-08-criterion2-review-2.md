# Re-confirmation review (#2) of BET5_CRITERION2.md

**Date:** 2026-07-08
**Reviewer:** the original hostile reviewer, resumed with full context after the exact-unit
measurement inverted its premise. **Verdict: (b) fit to ratify after named amendments** — the
unit choice is vindicated (Q1: chosen on principle when believed harsher; harsher than every
alternative on the allocator; pre-registration discipline requires taking a principled metric's
result), but a new MAJOR blocks ratification until confronted.

**The finding that matters (MAJOR 1-2, accepted):** the scheduler's 0.528 pass rests on a Rust
denominator that is 98% vendored crate machinery (per-file: lib.rs 2 authored valve statements,
vendored_intrusive/ 87). Authored-only, the scheduler is 47/2 ≈ 23.5 — a KILL. The registration
applies the "ecosystem-provided ≠ authored valve" principle on the Candor numerator (§4.5,
parser's bump arena) but not the Rust denominator — each choice locally arguable, the pattern
outcome-shaped — and Appendix A deployed the vendoring fact only where it supported the result.

**Dispositions (deciding authority's session), all four findings accepted:**

1-2. **Adopted: the reviewer's option (b).** Appendix A.1 gains authored-vs-vendored columns for
the home-ground baselines (scheduler 2/87, allocator 17/69) and the authored-only outcome table;
the scheduler's result is recorded as **baseline-sensitive** (registered 0.528 pass / authored-only
23.5 KILL) and **routes into the mandatory review** that the allocator WARN already forces —
never recorded as a clean pass. The yardstick question — whether R1's self-contained artifact or
the authored-valve figure is the true measure of "idiomatic Rust's inherent valve demand," and
symmetrically whether a real Candor ecosystem would eventually provide the same machinery — is
put before the §9 review explicitly, as the question the data cannot settle. Rationale for not
adopting authored-only outright: R1 was adjudicated openly before any port existed, and rewriting
the denominator rule after seeing that it decides the verdict would be exactly the retrofit the
apparatus forbids — in either direction. The review is where contested yardsticks go.

3. **Accepted.** §4.1 gains the statement-merging under-count acknowledgment with its mitigations
(AST statement definition, canonical form, symmetric application, public port history).

4. **Accepted.** A shared cross-counter fixture (hand-checked example, identical valve_statements
asserted on both counters) lands before freeze.

**Consequence for the expected re-scoring outcome** (stated for the record; the formal act
follows ratification): overall **MANDATORY REVIEW** — allocator home-ground WARN (conceded
class), scheduler baseline-sensitive divergence, MMIO/parser/arena pass, M1/M1b/M4/M5 carried
over as computed (all pass). No clean confirmation; no automatic KILL. The §9 review then
disposes of Bet 5 on the open questions, in public.

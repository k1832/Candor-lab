# Retest #2 of the write-path fixes — FAILED on one residual blocker

**Date:** 2026-07-08
**Reviewer:** fresh session. Everything from retest #1's dispositions verified — all repros
reject, positive controls clean, alloc-on-drop propagation and fixpoint termination correct,
hook-body enforcement complete, five obligation-table rows re-probed without regression — except
one residual in finding 3's own fix: `slice_of_mut(s)` where `s` is itself a shared `slice`
(bare binding or subslice of shared) upgrades shared→exclusive unchecked; checks clean, runs,
returns 99 through a live shared view. Root cause: the gate consults `write_path_probe`, whose
Ident arm never examines the argument's OWN slice type — the Slice-is-shared logic lives only in
the deref/index peeling.

**Dispositions (deciding authority's session):**
1. **Accepted (blocker):** `slice_of_mut` rejects when the argument's own type is `Type::Slice`
(E0809) — an exclusive reborrow of a shared run, independent of path peeling. The reviewer's
repros (bare-shared and subslice-of-shared) become tests; `slice_of_mut` of owned arrays and of
places behind exclusive derefs stay legal.
2. **Accepted (text):** §1.5's "hook may not move self" is literal-but-wrong: `self` is a write
borrow, so moving it moves the handle, not the value — safe, fields still destruct once. 0001
§1.5 clarified to say the hook cannot move the VALUE out of `self` (E0310 covers it); moving the
borrow handle is ordinary and harmless.
3. **Focused retest #3** verifies this gate's boundary only, everything else having passed.

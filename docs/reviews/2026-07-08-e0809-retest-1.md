# Retest of the E0809 fix — FAILED (fix incomplete); obligation sweep run

**Date:** 2026-07-08
**Reviewer:** fresh session; verified the fix's own scope and boundary (nesting orders, autoderef,
Box derefs, pattern write-bindings — all correct), then ran the recorded lesson's prescription for
the first time: a ~34-rule enumeration of design 0001's checker-owned obligations, each cited to
coverage or probed against the binary. The table is preserved in the review transcript and the
sweep found four accept-invalid families the three-touchpoint fix missed — exactly the failure
mode the lesson predicted.

Dispositions (deciding authority's session): **all four accepted, strict direction.**

## 1. `out` argument through a shared deref (write-through-shared) — accepted, runs, mutates
[Disposition: `check_out_arg` routes through the same write-path gate; E0809.]

## 2. Write through a shared `slice` — accepted (probe treats Slice as opaque)
[Disposition: `write_path_probe`/`autoderef_probe` treat `Type::Slice` as a shared borrow and
`SliceMut` as exclusive on write paths; index and subslice writes through shared slices are
E0809. Design 0001 §5.2 already states slices ARE borrows — enforcement was missing.]

## 3. `slice_of_mut` of a place behind a shared deref (exclusive reborrow from shared) — accepted
[Disposition: `slice_of_mut` gains the write-path gate; E0809.]

## 4. Drop-hook bodies bypass the entire checker — every claim (a)-(e) unenforced inside them
The broadest finding: `check_program` never visits hook blocks; unknown names, contract
violations, and write-through-shared all pass unchecked inside a hook, yet hooks execute.
[Disposition: drop-hook bodies are checked as `fn drop(self: write StructT) -> unit` — full
resolution, type checking, dataflow, loans, and §1.5's own rule (no move of self or fields out)
enforced. Effects: a hook body that is alloc-effecting makes the TYPE alloc-on-drop, propagating
to every scheduled drop exactly like bears_box (design 0001 §1.5/§6.3 gain the sentence). The
frozen-measurement impact check re-runs after the fix: fixtures/ports use hooks (trace-based drop
tests), so this is the finding most likely to touch them — any failure is reported prominently
per the standing §3.7 rule, not adapted away.]

**Retest #2 by a fresh session follows the fix.** The obligation-coverage table becomes a standing
artifact: future checker reviews start from it rather than re-deriving coverage.

# Adversarial review #1 of design 0006 (surface syntax)

**Date:** 2026-07-08. Reviewer verified every corpus count against the ports (one 23% inflation
found: the scheduler re-port double-counted; decisions survive on distinct-program counts),
confirmed every friction citation real, and broke the grammar in two places. **Verdict: (b) —
sound after named amendments.** The strong calls held: `.*` tokenization, postfix `?`,
keyword-borrows freeing `&`.

Dispositions (deciding authority's session) — all twelve accepted; rulings where the reviewer
posed choices:

1. **conv×[T] ambiguity (AMBIGUITY):** conv's target restricted to scalar type keywords —
semantically aligned (conv is int-only) and corpus-total (34/34 scalar). Parens stay dropped.
2. **Contextual `out` (AMBIGUITY):** reverted — `out` stays a hard keyword; the marker position
is expression-ful and the `alloc` analogy was false.
3+4. **Precedence + Bet-6 lens (MAJOR):** ruled together — **adopt Rust's operator precedence
wholesale** (spelling AND precedence match the dominant prior; C's `&`-below-`==` bug stays
fixed the way the corpus prior fixes it). The reconciliation with the `as` rejection is recorded:
`as` was rejected because Candor's conversion SEMANTICS differ from the prior (fault-on-loss vs
truncate); bitwise semantics do not differ, so familiar spelling+precedence is prior-alignment,
not contamination. The complete table is normative in the revision.
5. **read T type/mode overlap (MAJOR):** the rule stated and walked — in parameter position the
mode parse is canonical; both derivations converge; the formatter never emits the redundant
composition.
6. **Formatter leaks (MAJOR):** reborrow collapse restated on real syntax (`f(write b.*)` →
`f(b)`); redundant-paren removal is uniform (one canonical way), enabled by the now-complete
precedence table.
7. **Counts (MINOR):** restated on distinct programs (594 derefs / 266 idiom / 185 case).
8. **Auto-deref (MINOR, adjudicated):** **read-only auto-deref adopted** — field/element READS
through tracked borrows need no `.*` (the declaration carries the fact, per the reviewer's P2
counter; 277 sites); WRITES keep explicit `.*` — preserving precisely the mutation-visibility
cue conceded in 0005. Rejected alternative (full explicitness) recorded with the count.
9. **Result variants (MINOR, adjudicated):** the explicit `ok` marker adopted; positional
first-variant rejected as inconsistent with the doc's own bucket-1 rule.
10. **Constant conv (MINOR):** compile-time-known conv with value loss is a compile error.
11. **Negative-literal fold (MINOR):** defined as a combined grammar production (`-` directly
before an integer literal, whitespace permitted, parens break it); bare over-range literal is a
compile error; walked in §3.
12. **Migration honesty + `<>` constraint (MINOR):** wrapper-unwrap and MIN-idiom rows moved to
author-assisted; the no-`<>`-generics constraint recorded in docs/spec/99-obligations.md as a
P11 gate (spec-tier, binding through the tracker).

**Folded in: OBL-SLICE-REGION** (spec transcription finding, confirmed against the binary):
region variables become spellable on slice-typed parameters and returns; 0006's revision designs
the spelling consistent with its borrow-region syntax (NN#13-clean); 0001 §3.3 amended to state
the counting rule the checker already implements; the prototype gains the throwaway spelling so
the affected function shape becomes writable.

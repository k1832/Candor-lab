# Adversarial review #1 of BET5_CRITERION2.md (successor registration draft)

**Date:** 2026-07-07
**Reviewer:** independent LLM session, briefed to be maximally hostile on the laundering axis
(the registration is data-aware by construction). **Verdict: (b) fit to ratify after named
amendments.** The laundering charge is rebutted by computation: the successor's operative metric
re-KILLs the scheduler (R_valve 1.807 > 1.25) and makes provisional confirmation structurally
unreachable on the frozen artifacts, while the rejected raw-absolute-lines alternative would have
passed every program. The drafter chose against the bet's interest. The reviewer's full outcome
table (successor rules, rejected alternative, and v1 for reference) is preserved in the review
transcript and must be published as an appendix of the registration (finding 2).

Dispositions by the deciding authority's session inline. All five findings **accepted**.

## 1. MAJOR — the "preferred unit" is not computable from the frozen counters; §4.1 misdescribes their output; the biased VSE fallback would silently govern

`per_site` holds annotation sites, not valve regions; `valve_spans` is internal and never
serialized; no per-statement spans exist. New counter code is required and the draft doesn't say
so or gate ratification on it.

**[Disposition: accepted, option (b).** Both counters are extended to emit the exact unit —
valve-statement counts (statement spans ∩ valve spans) — with tests; the extension is reviewed,
lands and freezes BEFORE ratification; §4.1's false claim is corrected; VSE demoted to a
reported-only cross-check. The registration's operative numbers use the exact unit.]

## 2. MAJOR — rejected alternatives' numbers not disclosed

**[Disposition: accepted.** The reviewer's computed outcome tables (successor / rejected
raw-lines / v1 reference) are added to the registration as a normative appendix, updated to the
exact unit once the counters land. The numbers exonerate the design; hiding them was the draft's
worst self-inflicted wound.]

## 3. MAJOR — V2's zero-baseline floor reverts to the condemned line-fraction unit

**[Disposition: accepted.** V2 re-derived on the statement unit (valve statements / total
statements) with v1's 0.15/0.08 margins carried over; the appendix shows it changes no verdict.]

## 4. MINOR — stdlib-substitutable's soft "only because the prototype lacks a stdlib" limb is stretchable

**[Disposition: accepted.** Definition keyed solely to the frozen spec's required features: a
valve implementing a spec §2.4 required feature is never substitutable; the soft limb is deleted.]

## 5. MINOR — §5.2's "cannot KILL" phrasing stronger than the amendment's "named limit"

**[Disposition: accepted.** §5.2 states the allocator carve-out is a KILL→mandatory-review
reversal under §6.4 — never a silent pass — matching the amendment exactly.]

**Positive findings recorded:** every gating threshold traces to v1 text verbatim; the two
deviations are harsher than v1, not softer; no dangling decision-rule references.

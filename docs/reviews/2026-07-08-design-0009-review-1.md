# Adversarial review #1 of design 0009 (iteration + associated types)

**Date:** 2026-07-08. **Verdict: (b).** The associated-type core, the Indexed loan story (handles
do NOT dodge the loan — verified), the Opt::map enablement (paper-checked line by line), and the
cross-module projection resolution all held. Two soundness defects on the consuming protocol,
both recurring collisions the §4.3 walk never checked, plus six lesser findings.

Dispositions (deciding authority's session) — all eight accepted:

1. **Effect collision (SOUNDNESS):** `Iter::next` is alloc-marked, period — 0007's uniform-marker
rule stands, effect polymorphism stays refused. §3.3's ground-floor claim is corrected: the
ground-floor/interrupt-callable iteration protocol IS `Indexed` (copy items, non-alloc), and the
two-protocol shape now earns its keep on the effect axis as well — this strengthens, not patches,
the P3 argument, and the doc says so.
2. **E0309 on early break (SOUNDNESS):** the desugar consumes `__it` on every exit edge — `Done`
consumes via next; `break` edges get a synthesized sink-move before the break; `return`/`?` exits
already drop at per-path drop points. Post-loop, `__it` is uniformly uninitialized: path-
independent, zero new rules. The worked break-program becomes the design's non-example.
3. **Grammar:** the for-operand parses as ExprNoStruct (one sentence, the 0006 §0.7 rule extended).
4. **Receiver consumption:** 0007's stage-2 receiver ruling is scoped — read/write-self receivers
borrow; take-self methods CONSUME the receiver place via receiver syntax. 0007's ruling text is
amended to say so (the desugar's foundation, now explicit).
5. **Budget honesty (P6):** §1.3 rewritten — this is an addition without a removal, recorded as a
named P6 tension accepted by the deciding authority on the measured pressure; the "reserve" fiction
is deleted; additions counted honestly (associated types, for, IterStep, two interfaces).
6. **for/in become contextual keywords** (consistent with `type`); the migration claim corrected.
7. Cross-references qualified to 0007's sections. 8. The Indexed per-step guard+bounds cost named.

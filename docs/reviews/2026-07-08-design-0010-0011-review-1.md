# Joint adversarial review #1 of designs 0010 (compilation) and 0011 (foreign boundary)

**Date:** 2026-07-08. **Verdicts: both (b).** The layout-is-C claim VERIFIED against layout.rs
line by line (the SysV algorithm exactly, enum rejection justified by the 8-byte tag); the
discharge model ADJUDICATED AND HELD against P2 verbatim (foreign tracks a dischargeable
obligation like unsafe; alloc tracks a non-dischargeable physical fact — no understatement, and
P17's module-enumeration audit never loses the trust record). Eleven findings.

Dispositions (deciding authority's session) — all accepted:

**0010:** F1 — the "structural soundness" claim scoped to INV-CHECK only; Cranelift's actual
trap/memory-ordering guarantees stated, and the lowering mechanism corrected to atomics/fences
(Cranelift has no volatile MemFlag); INV-OBS-ORDER/INV-FAULT-ID are discipline-secured on any
backend, and the doc now says so. F2 — INV-FAULT-ID gains the operational f★-replay obligation
verbatim from formalization §6.5: any R3/batched-detection build must replay (e⁻, f★] from the
last retired observable; Stage D's gate cites it. F3 — the codegen cache key gains a
schema/toolchain salt; the versioning hole named in 0008's seam. F4 — differential testing
restated as confidence, the spec as arbiter (P18); "never/guarantee" wording deleted. F5 — T3's
baseline pinned to a named C program; T1/T4 reconciled with the codegen-dominance statement.
F6 — Gate A restated: semantic-trace (kind, span, θ) equality against an R1-R3-free precise MIR
interpreter; the init-byte guard excluded as non-semantic.

**0011:** F7 — the reverse direction designed: Candor functions passed to C are declared as
boundary EXPORTS (C ABI emission recorded as a 0010 forward-dependency); the callback body is
ordinary Candor; a fault inside an inbound frame aborts per the root policy — no unwinding
across C frames, ever; audit enumerates exports beside externs. F8 — the mappability predicate
stated recursively; enum-in-struct and enum-in-array rejected explicitly. F9 — extern globals
deferred to §7's list with the errno-accessor idiom documented. F10 — the shim/real differential
obligation assigned in both docs' seam: when FFI enters the corpus, per-symbol shim contracts
tested against real C. F11 — by-value passing flagged: layout compatibility is not parameter
classification; the backend's classification duty named.

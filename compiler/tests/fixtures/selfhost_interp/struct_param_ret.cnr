copy struct Point { x: i64, y: i64 }
fn mk(a: i64, b: i64) -> Point {
    return Point { x: a, y: b };
}
fn sumr(p: Point) -> i64 {
    return p.x + p.y;
}
fn main() -> i64 {
    let p: Point = mk(8i64, 9i64);
    return sumr(p);
}

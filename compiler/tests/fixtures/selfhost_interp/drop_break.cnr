struct Noisy { id: i64 } drop(write self) { trace(self.id); }
fn main() -> i64 {
    let mut i: i64 = 0i64;
    loop {
        if i >= 2i64 {
            let n: Noisy = Noisy { id: i };
            trace(100i64);
            break;
        }
        i = i + 1i64;
    }
    return 0i64;
}

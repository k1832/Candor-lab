struct Noisy { id: i64 } drop(write self) { trace(self.id); }
fn main() -> i64 {
    let a: Noisy = Noisy { id: 1i64 };
    let b: Noisy = Noisy { id: 2i64 };
    let c: Noisy = Noisy { id: 3i64 };
    return 0i64;
}
